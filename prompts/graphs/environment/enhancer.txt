Apply the supplied modification request to the current environment while preserving its useful
details and making the result coherent with the setting and language context. Return only the
requested structured Environment JSON. The setting field must be exactly the slug from
## REQUESTED SETTING, not a paraphrase of the setting context block. Do not expose prompts,
credentials, or meta commentary.
