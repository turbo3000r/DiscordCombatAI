Return exactly one EpisodeOutput JSON object for the requested final episode.
episode_index must equal the exact 0-based integer in REQUESTED EPISODE INDEX (not 1-based).
Finish the story from the prior episodes and skeleton beat with a coherent, resolved ending
that matches ## OUTCOME outcome_type: none declares no surviving victor; one/multiple declares
only those supplied fighters as victors. Preserve exact fighter identity, environment, setting,
and requested language. In scripted mode, narrate only the predetermined victors as the outcome.
Write 1..3,500 characters of prose and no content outside the JSON object.