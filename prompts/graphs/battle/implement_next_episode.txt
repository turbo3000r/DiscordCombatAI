Return exactly one EpisodeOutput JSON object for the requested middle episode.
episode_index must equal the exact 0-based integer in REQUESTED EPISODE INDEX (not 1-based).
Continue directly from the supplied earlier episode prose and the requested skeleton beat. Keep
fighter, environment, setting, chronology, and requested language coherent. Write 1..3,500
characters of narrative text only; do not introduce unlisted fighters, resolve the ending
prematurely, or include explanations outside the JSON object. Narrative prose must use fighter
names only and must never refer to Discord nicknames, player IDs, account identifiers, or
neutral Fighter labels.