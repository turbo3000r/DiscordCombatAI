Return exactly one EpisodeOutput JSON object for the requested middle episode_index. Continue
directly from the supplied earlier episode prose and the requested skeleton beat. Keep fighter,
environment, setting, chronology, and requested language coherent. Write 1..3,500 characters
of narrative text only; do not introduce unlisted fighters, resolve the ending prematurely, or
include explanations outside the JSON object.