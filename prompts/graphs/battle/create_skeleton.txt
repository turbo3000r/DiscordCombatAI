Return exactly one SkeletonOutput JSON object. Create exactly the episode_count from
## REQUESTED EPISODE COUNT and ## OUTCOME (2..5). episode_index values must be contiguous
0-based integers matching that count. Each summary is a concise 1..500-character beat.
Preserve every supplied fighter's identity, the supplied environment and setting, and the planned
outcome. In scripted mode, arrange the ending so the predetermined neutral Fighter labels are
victors. Narrative summaries must use fighter names only; never use Discord nicknames,
player IDs, account identifiers, or neutral Fighter labels. Return no prose outside the JSON object.