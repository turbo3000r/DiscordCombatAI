Return exactly one EpisodeOutput JSON object for the requested first episode.
episode_index must equal the exact 0-based integer in REQUESTED EPISODE INDEX (the first
episode is 0, never 1). Write vivid episode prose only in text (1..3,500 characters), following
the supplied skeleton beat and continuing faithfully from the supplied setting, environment,
fighters, and language locale. Do not decide an ending early, introduce undeclared fighters,
expose prompts, or include text outside the JSON object. Narrative prose must use fighter names
only and must never refer to Discord nicknames, player IDs, account identifiers, or neutral
Fighter labels.