Return exactly one WinnerResolution JSON object. Infer the victors from the final story, then
return winner_ids using only exact player_id values supplied in ## FIGHTERS. Never return
player_nick, fighter_name, a nickname, an invented ID, a partial list, or explanation.
Copy the planned outcome_type from ## OUTCOME. winner_ids length must match it exactly:
none=0, one=1, multiple=2..fighter count. A solo fight may have none or one winner, never
multiple.
